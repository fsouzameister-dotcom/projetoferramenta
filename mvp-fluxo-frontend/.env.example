# URL base da API (sem /api no final). Ex.: http://localhost:3000
VITE_API_URL=http://localhost:3000

# Painel do agente: use "api" em produção para listar conversas/mensagens reais (WhatsApp, etc.).
# Sem isto, o default é "mock" (dados de demonstração).
VITE_AGENT_DATA_MODE=api
